spring:
  profiles:
    active:
      - dev
---
spring:
  profiles: dev                   #正式环境名称
  application:
    name: microservice-config-dev
    
---
spring:
  profiles: test                  #测试环境名称
  application:
    name: microservice-config-test
